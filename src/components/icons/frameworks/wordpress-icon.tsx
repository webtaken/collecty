"use client";

export const WordPressIcon = ({ className }: { className?: string }) => (
    <img
        src="https://cdn.simpleicons.org/wordpress"
        alt="WordPress"
        className={className}
    />
);
